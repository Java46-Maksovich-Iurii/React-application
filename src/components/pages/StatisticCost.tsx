import React from "react";
import { coursesService } from "../../config/service-config";
const StatisticCost: React.FC = () => {
    return <label style={{fontSize: 40}}>
        Statistic Cost page is working
    </label>
}
export default StatisticCost;