import React from "react";
import { coursesService } from "../../config/service-config";
const StatisticHours: React.FC = () => {
    return <label style={{fontSize: 40}}>
        Statistic hours page is working
    </label>
}
export default StatisticHours;